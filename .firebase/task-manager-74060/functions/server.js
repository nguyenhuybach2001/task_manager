import { onRequest } from 'firebase-functions/v2/https';
  const server = import('firebase-frameworks');
  export const ssrtaskmanager74060 = onRequest({"region":"asia-southeast1"}, (req, res) => server.then(it => it.handle(req, res)));
  